import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'VietAgri Exchange - Vietnam Agricultural Marketplace',
  description:
    'Source premium Vietnamese agricultural products directly from certified producers. Quality guaranteed, export-ready.',
  openGraph: {
    title: 'VietAgri Exchange',
    description: "Vietnam's Premier Agricultural Marketplace",
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
